version: "3"
services:
  servidorweb:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "8044:80"
    command: chmod a+rxw /var/www/html
    command: chmod a+rxw /var/log/apache2
    volumes:
      - /root/iaw/PHP:/var/www/html
      - /root/iaw/logs:/var/log/apache2
    depends_on:
      - "bbdd"

  bbdd:
    image: mysql:5.6
    restart: always
    environment:
      MYSQL_DATABASE: wp_db
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_PORT: 3306
    volumes:
      - /root/iaw/.db_data:/var/lib/mysql
      - /root/iaw/scripts/clientProdct.sql:/docker-entrypoint-initdb.d
    ports:
      - 8045:3306

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    environment:
      PMA_HOST: "mysql"
      PMA_USER: "root"
      PMA_PASSWORD: "secret"
    depends_on:
      - "bbdd"
    ports:
      - "8081:80"
